import { BaseService } from "../base";
import { BaseServiceParams, ChatCompletionRequest, ChatCompletionResponse, ListModelsResponse, LlmGatewaySpeechUnderstandingRequest, LlmGatewaySpeechUnderstandingResponse } from "../..";
/**
 * The LLM Gateway service: an OpenAI-compatible chat completions API plus
 * AssemblyAI's Speech Understanding endpoints.
 */
export declare class LlmGatewayService extends BaseService {
    /**
     * Create a new LLM Gateway service.
     * @param params - The parameters to use for the service.
     */
    constructor(params: BaseServiceParams);
    /**
     * Create a chat completion.
     * @param request - The chat completion request. `stream: true` is not
     * supported by this client yet.
     * @throws LlmGatewayError when the request fails.
     */
    chatCompletions(request: ChatCompletionRequest): Promise<ChatCompletionResponse>;
    /**
     * List the models available on the LLM Gateway.
     * @throws LlmGatewayError when the request fails.
     */
    listModels(): Promise<ListModelsResponse>;
    /**
     * Run a Speech Understanding request.
     * @throws LlmGatewayError when the request fails.
     */
    understanding(request: LlmGatewaySpeechUnderstandingRequest): Promise<LlmGatewaySpeechUnderstandingResponse>;
    /**
     * Validate a Speech Understanding request without running it.
     * @throws LlmGatewayError when the request is invalid.
     */
    validateUnderstanding(request: LlmGatewaySpeechUnderstandingRequest): Promise<void>;
}
