import { BaseServiceParams } from "..";
import { SyncTranscriber } from "./sync";
import { LlmGatewayService } from "./llm-gateway";
import { SyncTranscriptError, LlmGatewayError } from "../utils/errors";
import { RealtimeTranscriber, RealtimeTranscriberFactory, RealtimeService, RealtimeServiceFactory } from "./realtime";
import { TranscriptService } from "./transcripts";
import { FileService } from "./files";
import { StreamingTranscriber, StreamingTranscriberFactory, DualChannelCapture, EnergyVad, LinearResampler, VadTimeline, attributeTurn, attributeWord, rollUpTurnChannel, float32ToPcm16 } from "./streaming";
declare class AssemblyAI {
    /**
     * The files service.
     */
    files: FileService;
    /**
     * The transcripts service.
     */
    transcripts: TranscriptService;
    /**
     * The realtime service.
     */
    realtime: RealtimeTranscriberFactory;
    /**
     * The streaming service.
     */
    streaming: StreamingTranscriberFactory;
    /**
     * The synchronous transcription service.
     */
    sync: SyncTranscriber;
    /**
     * The LLM Gateway service.
     */
    llmGateway: LlmGatewayService;
    /**
     * Create a new AssemblyAI client.
     * @param params - The parameters for the service, including the API key and base URL, if any.
     */
    constructor(params: BaseServiceParams);
}
export { AssemblyAI, SyncTranscriber, SyncTranscriptError, LlmGatewayService, LlmGatewayError, RealtimeTranscriberFactory, RealtimeTranscriber, RealtimeServiceFactory, RealtimeService, TranscriptService, FileService, StreamingTranscriber, DualChannelCapture, EnergyVad, LinearResampler, VadTimeline, attributeTurn, attributeWord, rollUpTurnChannel, float32ToPcm16, };
